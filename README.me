function setup() {
    createCanvas(400, 400);
    background(220);
    // HSB with H range 0-400, S range 0-400, B range 0-100:
    colorMode(HSB, 400, 400, 100);
}

function draw() {
    let x = random(width);
    let y = random(height);
    fill(h, s, 100);
    noStroke()
    ellipse(h, s, 20);
}

There is no change when I change "x","y" to "h","s" or "cat","dog"
